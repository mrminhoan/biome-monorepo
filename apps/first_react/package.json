{
  "name": "first_react",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "biome check src",
    "format": "biome check --write src"
  },
  "dependencies": {},
  "devDependencies": {
    "@repo/ui": "*"
  }
}
